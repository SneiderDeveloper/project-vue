export default {
    data() {
        return {
            note: `Esto esta en un mixin. Los mixins ya 
            no son recomendables con la Composition API.`,
            description: `Los mixins sirven para reutilizar
            código, exactamente para reutilizar las variables
            reactivas en data(), las funciones en methods(),
            variables computadas en computed, los watch, el
            provide. En general cualquier variable o función
            que se tenga dentro de la etiqueta <script>.`
        }
    }
}